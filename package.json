{
  "name": "Systems-Development-and-Frameworks",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "web:serve": "cd webapp && npm run serve",
    "web:build": "cd webapp && npm run build",
    "web:lint": "cd webapp && npm run lint",
    "web:test": "cd webapp && npm run test"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
